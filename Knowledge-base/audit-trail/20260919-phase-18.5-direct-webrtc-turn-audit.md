# Phase 18.5 Direct WebRTC + TURN as-built audit

**Date:** 19 September 2026
**Scope:** Synthetic implementation and current linked-database verification; no real-user activation
**Repository:** Orion, branch `codex/phase-18.5-direct-webrtc-turn`

## Outcome

Phase 18.5's authorized implementation slice is implemented and verified against local services and
the existing linked database. It contains a
disabled-by-default server admission boundary, appointment-scoped Direct WebRTC sessions, a
dedicated in-memory signaling gateway, short-lived TURN REST/HMAC credential issuance, an admin
kill switch, a protected responsive meeting route, and explicit desktop/mobile two-party coverage.
No real-user activation, TURN/signaling provisioning, Edge Function deployment, or website deployment
was performed. The three Phase 18.5 database migrations were applied to the linked Supabase project
after explicit user authorization and verified against the remote migration list.

## Implemented boundary

- `video_sessions` and the service-role-only access/control RPCs enforce the booked appointment,
  assigned patient/psychiatrist relationship, 15-minute early-open window, session end, and kill
  switch. Admins and unrelated participants are denied.
- `video-session-access` signs short-lived signaling admission and TURN credentials without storing
  signaling payloads or browser secrets; `video-control` exposes the admin-only toggle boundary.
- The synthetic signaling gateway accepts one patient and one assigned psychiatrist, forwards only
  offer/answer/ICE setup messages, bounds message size, handles heartbeats/leave, and supports real
  SDP-sized WebSocket frames.
- The UI requires an explicit camera/microphone action and uses the Direct WebRTC route only when the
  advisory UI flag is enabled; server admission remains authoritative. The historical JaaS route is
  unchanged and remains the default.

## Verification evidence

- `npm ci`: completed with 0 audit vulnerabilities.
- `npm run test:unit`: 7 passed, including the signaling boundary test.
- `npm run test:load:phase185`: passed with 30 synthetic identities, 10 concurrent signaling
  sessions per wave, 20 peak participants, and two waves exercising all 30 identities.
- `npm run lint`: passed; phase-status indexes synchronized.
- `npm run build`: passed; initial JavaScript bundle 170.02 kB gzipped against a 180 kB budget.
- `supabase db lint --local`: passed with no schema errors.
- `supabase db lint --linked`: passed with no schema errors.
- `supabase db reset --local --yes`: passed through all three Phase 18.5 migrations.
- `npm run test:db:phase185`: passed shared-session, relationship-denial, and kill-switch checks.
- `supabase db advisors --linked --type security --level info --fail-on error`: completed with
  existing warnings for RLS-without-policy tables, callable security-definer functions, OTP expiry,
  and leaked-password protection.
- `supabase db advisors --linked --type performance --level info --fail-on error`: completed with
  informational index findings.
- Existing `npm run test:db:rls`: passed the appointment relationship/projection and protected-role checks.
- Local public/authenticated Playwright suite: 58 passed and 20 intentionally skipped because the
  database-backed suite was not enabled for that run; Direct WebRTC route redirect coverage passed.
- Gated `RUN_DIRECT_WEBRTC_E2E=1` Playwright run: desktop Chromium and mobile Chromium both
  established a two-party call with fake camera/microphone devices; 2 passed.
- Local Edge Function serving compiled and loaded `video-session-access` with JWT verification
  configured in `supabase/config.toml`.

## Remaining release gates

This audit does not close the real-user activation gate. Signaling/TURN hosting and relay-path
testing, Edge Function deployment, website deployment from verified `main`, privacy/DPO and
clinical approvals, Phase 16/17 predecessor evidence, retention/telemetry policy, named operational
ownership, and a separate real-user go/no-go remain required before activation.
