export default function FeaturePlaceholder({ title }) {
  return (
    <section className="marketing-page">
      <p className="eyebrow">Orion</p>
      <h1>{title}</h1>
      <p className="marketing-lead">This role-specific area is protected, but its database-backed workflow is the next implementation slice.</p>
    </section>
  );
}
